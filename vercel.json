{
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/playos run build",
  "outputDirectory": "artifacts/playos/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
