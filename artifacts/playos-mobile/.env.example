# PlayOS mobile — local dev secrets. Copy to .env.local (gitignored).
# Native apps don't read process.env at runtime — these values must be
# written into app.json > expo.extra by the next model, OR loaded via
# babel-plugin-transform-inline-environment-variables. Use whichever the
# implementer prefers.

SUPABASE_URL=https://tnqlvszrcskvuqtdcded.supabase.co
SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY

# PostHog — same EU project as the web app.
POSTHOG_KEY=phc_AWm9NYadeyvRG4mASJPC8hcgNV8Ck37S9BAaywNtCZMv
POSTHOG_HOST=https://eu.i.posthog.com
